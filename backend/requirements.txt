# Core dependencies - lightweight and fast
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
python-dotenv==1.0.0
requests==2.32.3
beautifulsoup4==4.12.3

# Groq SDK for LLM
groq==0.13.0

# Note: We removed torch, sentence-transformers, numpy, scikit-learn, scipy
# These were using ~500MB of RAM just for the embedding model
# Now using Jina AI's free embeddings API instead - saves massive memory!
